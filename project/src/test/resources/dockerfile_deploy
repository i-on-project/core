FROM ghcr.io/i-on-project/flyway-postgres:latest

ARG COMMIT_SHA

# Echo the commit SHA (generated by GitHub Actions)
RUN echo $COMMIT_SHA > commit_sha
# Copy the necessary project resources
COPY ./project/res/ .

ENTRYPOINT [ "./bin/deploy" ]
